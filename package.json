{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.2.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run --stream test",
    "test-travis": "lerna run --stream test-travis",
    "dev": "lerna run dev --parallel"
  }
}
